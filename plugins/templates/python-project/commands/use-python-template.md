Copy the Python project CLAUDE.md template into the current working directory.

Steps:
1. Check whether a `CLAUDE.md` already exists in the current directory. If it does, warn the user and ask for confirmation before overwriting.
2. Read the template from `${CLAUDE_PLUGIN_ROOT}/CLAUDE.md`.
3. Write the contents to `CLAUDE.md` in the current working directory.
4. Tell the user which placeholder sections (marked with `<!-- -->` comments) they should fill in.

Do not modify the template content — copy it verbatim.
