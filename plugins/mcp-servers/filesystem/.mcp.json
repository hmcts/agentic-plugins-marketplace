{
  "mcpServers": {
    "filesystem": {
      "command": "sh",
      "args": ["-c", "npx -y @modelcontextprotocol/server-filesystem $FILESYSTEM_ALLOWED_DIRS"],
      "env": {
        "FILESYSTEM_ALLOWED_DIRS": "${FILESYSTEM_ALLOWED_DIRS}"
      }
    }
  }
}
