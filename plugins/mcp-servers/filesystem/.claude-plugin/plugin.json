{
  "name": "filesystem",
  "version": "1.0.0",
  "description": "Give Claude sandboxed read/write access to directories on your local filesystem",
  "author": {
    "name": "Anthropic"
  },
  "license": "MIT",
  "homepage": "https://github.com/modelcontextprotocol/servers/tree/main/src/filesystem",
  "keywords": ["filesystem", "files", "local", "read", "write"],
  "userConfig": {
    "FILESYSTEM_ALLOWED_DIRS": {
      "type": "string",
      "title": "Allowed directories",
      "description": "Absolute path(s) Claude can read and write. Separate multiple paths with a space (e.g. /home/user/projects /tmp).",
      "required": true
    }
  }
}
