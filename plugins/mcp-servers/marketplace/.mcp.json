{
  "mcpServers": {
    "marketplace": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/server.js"],
      "env": {
        "MARKETPLACE_DIR": "${CLAUDE_PLUGIN_ROOT}"
      }
    }
  }
}
